workflows:
  android-workflow:
    name: Android Workflow
    environment:
      java: 17
      gradle: 9.3.1
    scripts:
      - name: Build APK
        script: |
          gradle assembleDebug
    artifacts:
      - app/build/outputs/apk/debug/*.apk

